import { PaletteOptions } from "@mui/material";

type PlatteType = {
  dark?: PaletteOptions;
  light?: PaletteOptions;
};

const palette: PlatteType = {
  dark: {},
  light: {},
};

export default palette;
