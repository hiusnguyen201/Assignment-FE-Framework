export enum Gender {
  MALE = "Male",
  FEMALE = "Female",
  OTHER = "Other",
}

export enum PasswordNotice {
  VIA_EMAIL = "Send password via email",
  VIA_SMS = "Send password via sms",
  SHOW_PASSWORD = "Show password",
}

export enum AccountStatus {
  ACTIVE = "Active",
  INACTIVE = "Inactive",
  BLOCKED = "Blocked",
}

export enum Role {
  ADMIN = "Admin",
  CUSTOMER = "Customer",
}
